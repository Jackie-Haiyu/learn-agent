# 告警消息中心（Alert Message Center）需求文档

## 1. 系统定位与范围

告警消息中心用于在控制台中统一呈现“告警/异常事件”的列表、详情与处置联动能力，支撑以下使用路径：

- 告警数据接收&存储： 告警数据接收支持接口&消息队列（RocketMQ）， 告警数据存储使用ClickHouse。
- **告警管理页**：以卡片流形式查看告警列表，支持筛选与查看详情（含图片放大/全屏）。
- **实时监控页**：右侧“今日告警/历史告警”列表与视频画面联动，便于从告警快速定位监控画面与处置。
- **全链智判**（智能研判）：围绕“计划/任务/技能”的研判过程与结果实时展示，支持查看 SOP 执行步骤状态、查看视频关键帧/回放与失败原因定位。
- **全链智判 - 研判记录**：沉淀历史研判任务的查询与复盘能力，支持按时间/计划/任务/技能检索研判记录，查看单次研判的耗时、状态、关键帧/回放、步骤结果与失败原因定位。

### 1.1 V1 边界

- 本文档仅整理 UE 中已体现的产品能力与交互，不扩展未在 UE 中出现的“订阅规则/多渠道推送/模板管理”等后台能力。
- 权限控制、区域/组织隔离、告警来源对接方式等不在本次需求范围内，仅在字段中保留可扩展信息（如区域、来源系统等）。

### 1.2 Workflow 告警数据契约（与实现对齐）

- **RocketMQ JSON**：以概要设计《告警消息中心》**§3.0.1** 为准（`timestamp`、`id`、`image`、`custom`、`meta_data`、`targets`、`tags`）；`meta_data` 中统一使用 **`tenantId`、`sourceId`、`sourceName`** 等字段。
- **ClickHouse**：Workflow 告警落库表为 **`workflow_event`**（核心维度物理列 + 列名 `meta_data`/`custom`/`targets`/`tags`，列内 JSON 与 MQ 契约一致），详见概要设计 **§4.1** 与 `docker/init/clickhouse-init/V1__init.sql`。
- **主键**：业务上以消息 **`id`** 作为告警唯一标识（与 HTTP 返回的 `alertId` 一致）。
- **列表与详情**：告警列表项与详情与 **RocketMQ Workflow JSON** 同构（`timestamp`、`id`、`image`、`custom`、`meta_data`、`targets`、`tags`），详见概要设计 **§3.1**。
- **实现侧（与代码对齐）**：Workflow 告警内层 `meta_data` 对应 Java 类型 `WorkflowAlertMetaData`；研判/SOP 内层 `meta_data` 对应 `JudgeSopEventMetaData`，二者隔离。若 Workflow 根 JSON 仅有顶层 `metaData` 而无 `meta_data`，消费端会先规范键名再解析。

### 1.3 SOP 合规告警（judge）数据落地约束（新增）

- SOP 合规告警来自 RocketMQ，真实业务体在 `answer` 字段（JSON 转义字符串）中。
- 存储方案采用 **append-only + MV 聚合**：
  - 事件事实：`sop_event`
  - 实例查询：`sop_instance`
  - 节点查询：`sop_node_event`
- 查询口径：
  - 列表/统计按实例快照查询（`instance` 维度）
  - 详情步骤按节点快照查询（`node` 维度）
- `node_end.frame_urls` 需要转存对象存储并回写 `frame_keys`，不直接保存外部 URL。

---

## 2. 需求描述

### 2.1 告警管理（列表页）

#### 2.1.1 页面布局

- 页面标题：**告警管理**
- 主体为**卡片列表**（多列瀑布/网格），每张卡片包含：
  - **告警标题**（示例：`东南门日常巡检`，以醒目颜色展示）
  - **告警缩略图**（无图时展示占位图）
  - **告警类型/来源**（示例文本：`车间巡检流程检验`）
  - **点位/位置**（示例：`铺铜蓝冰智慧车间东南门`）
  - **告警时间**（示例：`2026-03-03 13:13:13`）

#### 2.1.2 筛选与查询

页面顶部筛选区（按 UE）：

- **创建时间**：起止时间选择（时间范围）
- **计划名称**：输入框（模糊匹配）
- **技能名称**：下拉选择（可清空）
- **重置**：清空筛选条件
- **查询**：按当前筛选刷新列表
- **展开/收起**：用于显示/隐藏更多筛选项（UE 有入口；V1 可先实现展开/收起容器）

列表分页/加载（按 UE 底部提示）：

- **最多显示 100 条**（超过后提示“到尾啦”，并提供“查看更多/历史告警”入口的交互一致性）

#### 2.1.3 列表交互

- 点击告警卡片，进入**告警详情弹窗**（见 2.2）。

---

### 2.2 告警消息详情（弹窗）

#### 2.2.1 弹窗内容

- 弹窗标题：**告警详情**
- 左侧：告警图片/画面大图展示（支持点击进一步全屏）
  - 顶部提示文案：`点击图片全屏查看`
- 右侧：**预警信息**（字段以 UE 为准）
  - 计划名称
  - 技能名称
  - 预警描述（示例：`实时分析`）
  - 预警时间（示例：`2026-02-01 12:00:00`）

#### 2.2.2 详情页内导航

- 右下角提供：**查看上一个/下一个报警**
  - 按钮：`上一个`、`下一个`
  - 语义：在当前筛选列表结果集中切换详情数据（无需回到列表）

---

### 2.3 告警图片全屏查看

#### 2.3.1 全屏模式样式要求（UE 标注）

- 全屏遮罩背景：**black 60%**
- 去除外层白色框
- 图片展示：
  - **宽度可扩展，不受限制**
  - **高度不超过**：上方操作栏（esc 行）与下方按钮组之间的可用高度
- 底部按钮组背景：**black**
- 支持 `ESC` 退出（UE 有“按 ESC 退出/提示”）

---

### 2.4 实时监控（右侧告警列表联动）

#### 2.4.1 页面关键区域（UE 范围）

- 页面标题：**实时监控**
- 中间：多宫格视频画面（2x2），支持点击点位开始播放。
- 右侧：**今日告警（数量）** 与 **历史告警** 入口
  - `今日告警（23）`（数量为未处理/今日告警数量，具体口径待后续确认；V1 以“今日产生的告警条数”展示即可）
  - `历史告警 >`（跳转至历史列表/更多页面）

#### 2.4.2 右侧告警卡片信息

每条告警卡片展示字段（与告警管理卡片一致）：

- 告警标题（如 `东南门日常巡检`）
- 告警类型/来源（如 `车间巡检流程检验`）
- 点位/位置（如 `铺铜蓝冰智慧车间东南门`）
- 告警时间

底部交互（UE）：

- 到底提示：`到尾啦`
- 规则提示：`最多显示100条`
- 入口：
  - `跳转` / `历史告警` / `查看更多`（UE 存在“查看更多/历史告警”链接表现；V1 保证至少有“历史告警”入口）

#### 2.4.3 与视频画面联动

UE 显示告警列表与视频播放存在联动预期（V1 定义）：

- 点击右侧某条告警：
  - 若告警关联点位/摄像头：在中间宫格中**自动选中并播放**对应点位视频（若当前未在播放则开始播放；若正在播放则切换到该点位）。
  - 若告警关联截图：可在右侧或弹窗中查看告警图片（复用 2.2/2.3 的详情能力）。

#### 2.4.4 实时推送订阅规则

为支撑“实时监控页”实时展示新产生的事件/告警数据，后端对 **Workflow 告警** 同时提供 **SSE** 与 **WebSocket**，推送 JSON 与 RocketMQ 消息体一致；**研判 / SOP 事件** 仍通过 **SSE** 推送。按“用户是否选择相机播放”动态控制推送范围：

1. **未选择任何相机（默认态）**：
  - 后端应向前端**推送所有接收到的新产生事件/告警摘要数据**（不按相机过滤）。
  - 前端右侧“今日告警”列表实时追加/刷新。
2. **用户选择了某个或某些相机播放（关注态）**：
  - 后端只推送**用户当前关注的相机集合**产生的告警/事件摘要数据。
  - 当用户切换关注相机集合时，前端关闭当前 SSE 连接，携带新的 `cameraIds` 参数重新建立连接，推送范围随之切换（允许短暂抖动，最终一致即可）。

补充约束（V1）：

- **关注相机集合为空的语义**：表示“默认全量推送”，而不是“不推送”。
- **推送数据粒度**：只推送“可用于右侧列表展示”的摘要字段；详情数据仍通过查询接口获取。
- **上限预留**：关注相机数量建议设置上限（如 50/100，具体按前端 UE 与性能评估确定），超过上限按参数非法处理。
- **动态订阅变更方式**：采用**断开重连**方式——前端关闭当前 SSE 连接，使用新的 `cameraIds` 参数重新建立连接。重连时携带 `sinceTime`（上次收到消息的时间戳）或利用 SSE 内建的 `Last-Event-ID` 补偿断连窗口内可能漏推的消息。

---

### 2.5 全链智判 - 智能研判（实时查看）

#### 2.5.1 实时查看页面

页面构成（UE）：

- 中间：视频画面（单画面为主）
- 右侧：**当前研判任务信息** + **SOP 流程步骤列表**（可滚动）
- 步骤列表项呈现：
  - 步骤序号
  - 步骤名称
  - 状态（UE 有绿色通过/红色失败/灰色未开始等表达）
  - 可展开查看步骤的时间与结果信息（V1 以“开始/结束/耗时/结果”展示为主）

#### 2.5.2 SOP 流程状态展示

UE 给出至少两类状态场景：

- **全部通过/正常执行**：右侧步骤均为通过状态
- **存在失败/异常结束**：
  - 右侧顶部显示异常/失败提示（UE 有红色区域提示）
  - 某些步骤展示为失败状态，并可查看失败原因或失败结果摘要

---

### 2.6 全链智判 - 研判记录（列表 & 详情）

#### 2.6.1 列表筛选（研判记录列表）

顶部筛选项（UE）：

- 时间范围（起止时间）
- 计划名称（输入框）
- 任务名称（输入框）
- 技能名称（下拉选择）
- 查询 / 重置

#### 2.6.2 列表字段（研判记录列表）

表格列（UE）：

- 计划名称
- 技能名称
- 任务名称
- 开始时间
- 结束时间
- 执行状态（通过/失败/异常结束等）
- 操作：查看详情

---

#### 2.6.3 研判记录详情 - 基础信息

顶部信息区（UE）包含：

- 标题：`研判记录 / 研判详情`
- 计划名称、技能名称
- 总耗时
- 开始时间、结束时间
- 执行状态（通过/失败/异常结束）

#### 2.6.4 研判记录详情 - 关键帧回放/视频回放

- 左侧为视频回放区域，具备播放控制条。
- 进度条下方/上方存在“关键帧”标记点（UE 有多个颜色点位）：
  - 代表不同步骤在时间轴上的关键节点。
  - 点击关键帧标记应跳转到对应时间点回放（V1 支持“跳转定位”）。

#### 2.6.5 研判记录详情 - 执行步骤列表

右侧为执行步骤列表（UE：`执行步骤（5）`）：

每个步骤项至少展示：

- 步骤序号 + 步骤名称
- 状态（通过/失败/执行中/未开始）
- 开始时间、结束时间、耗时
- 结果摘要（如“已保存”等）

#### 2.6.6 研判记录详情 - 失败数据展示（UE 场景）

当执行失败时（UE 示例）：

- 失败步骤项使用红色高亮，并展示失败原因/失败提示（如“未穿安全帽（置信度:0.96）”）
- 视频画面上叠加检测框与告警标签
- 支持查看失败步骤对应的关键帧与定位时间

---

## 3. 需求约束与一致性要求（V1）

1. **列表/详情一致性**：
  - 告警管理卡片与实时监控右侧告警卡片字段保持一致。
  - 告警详情弹窗在两处入口均可复用（列表点击/监控侧边点击）。
2. **最多显示 100 条**：
  - “今日告警”右侧列表最多展示 100 条，提供“历史告警/查看更多”入口。
3. **详情翻页**：
  - 告警详情支持“上一个/下一个”，顺序基于当前列表筛选结果集。
4. **全屏看图体验**：
  - 背景黑 60%，无白色外框；图片宽度自适应扩展，高度受上下栏约束；ESC 退出。
5. **告警实时推送必须实现**：
  - V1 采用 SSE（Server-Sent Events）实现“告警数据实时推送”能力，用于实时监控页右侧列表实时更新。
  - 默认态（未选择相机）必须支持“全量推送新产生告警摘要”；关注态（选择相机集合）必须支持“按关注相机集合过滤推送”。
  - 仅靠前端轮询接口不满足 V1 需求（查询接口用于详情/补拉，不替代推送链路）。
  - 动态订阅变更（如切换关注相机）采用断开重连方式，前端关闭当前 SSE 连接并携带新 `cameraIds` 重新建连，通过 `sinceTime` / `Last-Event-ID` 补偿断连期间漏推。

---

## 4. 验收标准（补充）

### 4.1 数据保留策略（ClickHouse + 对象存储）

1. **`GET /api/v1/datapool/retention`**：在 ClickHouse 可用时，响应中应能体现 Workflow 与 SOP 相关四张表（`workflow_event`、`sop_event`、`sop_instance`、`sop_node_event`）的 TTL 保留天数解析结果；在对象存储 Lifecycle 可读时，应能体现告警图片桶受管规则的过期天数（字段语义以概要设计 **§3.4.4** / **§4.4** 为准）。
2. **`PUT /api/v1/datapool/retention`**：
   - 支持 **`retentionDays`**：ClickHouse 四表与对象存储使用同一天数；
   - 支持 **`clickhouseRetentionDays` / `objectStorageRetentionDays`**：可分别设置；仅传一侧时另一侧默认继承同值；**不得**与 `retentionDays` 同时传入；
   - 取值范围 **1～3650** 天；非法组合或空请求应拒绝并返回可定位的错误信息。
3. **生效范围**：成功后 ClickHouse 侧应对上述四张表完成与请求一致的 TTL 调整；对象存储侧应更新告警图片 bucket 上受管 Lifecycle 规则（与 `megvii.s3.alert-image.lifecycle-rule-id` 等配置一致）。若对象存储步骤失败，应明确提示 ClickHouse 可能已变更，便于运维重试或手工对齐（行为见详细设计 **§4.5**）。
4. **存量环境**：允许运维不经过 HTTP，按概要设计 **§4.4** / 详细设计 **§3.3.1** 在 ClickHouse 执行一次性 `ALTER TABLE ... MODIFY TTL`，执行后可通过 `SHOW CREATE TABLE` 核对四表 TTL 表达式。

