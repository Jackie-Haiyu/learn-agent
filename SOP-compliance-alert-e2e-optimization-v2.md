# SOP 合规告警全流程优化方案（V2）

> **负责人**：张海玉  
> **日期**：2026-04-14  
> **版本**：v2.0

---

## 1. 优化目标

1. 处理链路简单可维护：实例归并器 + 单一规则引擎。
2. 存储链路高扩展：`sop_event/sop_instance/sop_node_event` 三表支持高吞吐写入与多场景查询。
3. 推送链路高稳定：SSE/WS 双通道并行，支持补偿与背压。
4. 运维链路可观测：异常可定位、可重放、可恢复。

---

## 2. 端到端优化清单

### 2.1 接入层

- 引入统一反序列化与标准化组件，消除业务层字符串 JSON 处理。
- 接入失败落 `raw` 与错误原因，避免吞消息。
- 使用分级幂等与弱去重，确保核心事件不重复、辅助事件可控降噪。

### 2.2 判定层

- 统一归并器处理实例与节点快照，实例状态直接沿用 `instance_end.status`。
- 统一规则引擎处理节点与实例告警，避免多处分支。
- 对乱序事件采用按事件时间回放策略，保证最终一致。

### 2.3 落库层

- 采用 `sop_event -> (MV) -> sop_instance/sop_node_event` 三表链路。
- 列表/详情/统计全部走 `sop_instance/sop_node_event`，不在线聚合原始事件。
- 按热冷数据生命周期配置 TTL，控制成本。

### 2.4 推送层

- SSE 与 WS 共用过滤引擎，避免双套逻辑。
- 默认推送摘要，详情按需查询。
- 支持前端断线自动重连并重发订阅条件。

### 2.5 可观测层

- 指标：消费延迟、快照更新耗时、告警命中率、推送成功率。
- 日志：按 `traceId + instanceId` 关联全链路。
- 告警：未完成实例占比、推送失败率、死信堆积阈值告警。

---

## 3. 分阶段实施建议

### Phase 1：三表模型切换

- 上线 `sop_event` 与实例归并器，不改前端接口。
- 双写旧表与新表（仅内部验证期使用）。

### Phase 2：查询与告警切换

- 切换实例列表、详情、告警查询到新快照表。
- 切换告警规则到新规则引擎。

### Phase 3：推送链路切换

- SSE/WS 统一走新过滤引擎。
- 打开补偿机制与背压策略。

### Phase 4：收敛与清理

- 停止旧链路写入。
- 清理兼容代码与冗余字段。

---

## 4. 验收指标

| 指标 | 目标 |
| --- | --- |
| 事件处理成功率 | >= 99.99% |
| 实时推送成功率 | >= 99.9% |
| 实例终态一致性 | 100%（可重算一致） |
| 列表查询 P95 | < 500ms |
| 告警推送延迟 P95 | < 1s |

