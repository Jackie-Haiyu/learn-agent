# SOP 合规告警重构需求文档（V2）

> **负责人**：张海玉  
> **日期**：2026-04-14  
> **版本**：v2.0

---

## 1. 背景与目标

### 1.1 背景

当前 SOP 合规事件来自流式 `agent_message`，业务事件包装在 `answer` 字符串中，并包含 `sop_start`、`instance_start`、`node_start`、`node_end`、`instance_end`、`sop_end` 等类型。  
新版示例已明确：

- `node.status`：`success` / `skip` / `redo` / `timeout` / `failed`
- `instance` 列表状态：直接使用 `instance_end.status`
- 前端订阅后，需按订阅条件筛选并通过 SSE 或 WebSocket 推送

### 1.2 重构目标

本次重构为开发中功能，**不做历史兼容**，目标如下：

1. 统一事件模型，移除多层字符串 JSON 对业务层的影响。
2. 以“消费、解析、存储、检索”为主线先完成主链路，状态机推断能力暂不纳入本期。
3. 完整支持 `SSE + WebSocket` 双通道实时订阅与过滤推送。
4. 基于新需求重建 ClickHouse 存储模型，提升查询效率与可维护性。
5. 全流程可观测，具备高稳定、可重放、可恢复能力。

---

## 2. 业务范围

### 2.1 范围内

- SOP 合规事件接入、解析、标准化、落库、查询、实时推送
- SOP 实例列表、实例详情（时间线/节点）、告警列表/详情
- 告警规则引擎（基于节点状态和实例终态）
- 前端订阅管理（连接、过滤、心跳、前端断线自动重连）

### 2.2 范围外

- 历史旧表兼容迁移
- 多租户权限体系重构（仅保留租户字段过滤能力）
- SOP 引擎本体行为改造（仅消费其输出事件）

---

## 3. 术语与状态定义

### 3.1 术语

- **SOP 实例（instance）**：一次流程执行记录，主键 `instanceId`
- **节点（node）**：实例内步骤，主键为 `instanceId + nodeId`
- **事件（event）**：流式输入事件，如 `node_end`
- **告警（alert）**：规则引擎产出的异常事实

### 3.2 节点状态

| 状态 | 含义 |
| --- | --- |
| `success` | 节点按预期完成 |
| `skip` | 跳步（后续节点先发生，当前节点被跳过） |
| `redo` | 补做（已跳过节点在后续阶段被补做） |
| `timeout` | 节点在约束时间内未完成 |
| `failed` | 节点判定失败（动作不符合、检测失败或规则失败） |

### 3.3 实例状态

| 状态值 | 含义 | 来源 |
| --- | --- | --- |
| `instance_end.status` 原值 | 实例执行状态 | 直接取自上游 `instance_end` 事件，不在本服务二次推断 |

---

## 4. 功能需求

### 4.1 事件接入与标准化

1. 系统必须消费 SOP 合规流式事件。
2. 系统必须支持将原始消息标准化为统一 Canonical Event。
3. 系统必须校验关键字段：`eventType`、`eventTime`、`instanceId`（节点事件还需 `nodeId`）。
4. 校验失败事件必须落入错误事件表并可查询追踪。

### 4.2 实例归并与去重

1. 系统必须以实例为维度归并事件，并生成实例快照。
2. 系统必须支持节点重复上报去重（核心事件按幂等键生效一次）。
3. 系统必须在收到 `instance_end` 后写入实例状态，状态值直接沿用上游。

### 4.3 告警规则

1. 系统必须支持节点级告警：
   - `skip` / `timeout` / `failed` 触发高优先级异常告警
   - `redo` 触发中优先级修复类告警
2. 系统必须支持实例级告警：
   - 当 `instance_end.status` 命中异常配置时触发实例告警
3. 系统必须支持告警去重窗口，避免同一实例同类告警风暴。

### 4.4 查询接口

1. 提供实例列表查询（按时间、计划、技能、来源、状态筛选）。
2. 提供实例详情查询（时间线、节点详情、异常列表）。
3. 提供告警列表与详情查询（支持按级别、状态、来源筛选）。
4. 列表接口分页能力必须支持高并发读场景。

### 4.5 实时推送（SSE + WebSocket）

1. 同时支持 SSE 与 WebSocket 双通道。
2. 订阅过滤条件至少包含：
   - `tenantId`、`planId`、`skillId`、`sourceId`
   - `instanceStatus`、`nodeStatus`、`eventType`
3. 支持连接心跳与前端断线自动重连。
4. 推送消息必须优先发送摘要，详情通过查询接口拉取。

---

## 5. 数据存储需求

### 5.1 存储边界

- ClickHouse：`sop_event`、`sop_instance`、`sop_node_event` 三表为主存储
- 关系库：本期不强依赖；如未来需要规则运营化再引入

### 5.2 数据能力要求

1. 支持原始事件可回放。
2. 支持按实例和节点高效检索。
3. 支持按告警维度聚合统计。
4. 支持数据生命周期管理（热数据快查、冷数据归档）。

---

## 6. 非功能需求

### 6.1 稳定性

- 事件采用分级幂等：核心业务事件强幂等，辅助观测事件弱去重。
- 处理链路支持失败重试和死信补偿。
- 推送链路支持背压控制和连接限流。

### 6.2 性能

- 实例列表 P95 查询延迟 < 500ms（常规筛选）
- 详情查询 P95 < 800ms（含节点时间线）
- 实时推送端到端延迟 P95 < 1s

### 6.3 可观测性

- 指标：消费延迟、去重命中率、终态归并耗时、推送成功率、连接数
- 日志：事件标准化日志、状态转移日志、告警触发日志、推送日志
- 链路：按 `traceId + instanceId` 贯穿全流程

---

## 7. 验收标准

1. 能正确消费并解析新版 SOP 样例事件流。
2. 节点状态与实例终态判定符合本文规则且可复算。
3. 实例状态与 `instance_end.status` 保持一致，不发生二次改写。
4. `SSE + WebSocket` 均可按订阅条件推送，并支持前端自动重连。
5. 查询接口满足列表、详情、告警三类核心页面。
6. 异常场景（乱序、重复）可稳定处理并可追踪。
7. **数据保留与生命周期**：与 Workflow 共用 **`GET/PUT /api/v1/datapool/retention`**（验收细则见主需求 `demand-1.md` **§4.1**）。SOP 侧须保证 `sop_event`、`sop_instance`、`sop_node_event` 与 `workflow_event` 一并纳入统一 TTL 策略（避免仅清理事件事实表、快照表长期残留）；运维存量对齐 SQL 见概要设计 **§4.4** / 详细设计 **§3.3.1**。

